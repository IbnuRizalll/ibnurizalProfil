---
const target = '/admin/about'
const hasAdminAccess = Astro.cookies.get('admin_access')?.value === '1'

return Astro.redirect(hasAdminAccess ? target : `/login?next=${encodeURIComponent(target)}`, 302)
---
